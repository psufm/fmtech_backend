// Global
const HOME = "/";
const LOGIN = "/login";
const LOGOUT = "/logout";
const GOOGLE = "/google";
const CALLBACK = "/google/callback";
const INDEX = "/index";
const routes = {
  home: HOME,
  google: GOOGLE,
  login: LOGIN,
  logout: LOGOUT,
  index: INDEX,
  callback: CALLBACK
};

export default routes;
