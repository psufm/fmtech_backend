// Global
const HOME = "/";
const LOGIN = "/login";
const LOGOUT = "/logout";
const GOOGLE = "/google";
const CALLBACK = "/google/callback";
const routes = {
  home: HOME,
  google: GOOGLE,
  login: LOGIN,
  logout: LOGOUT,
  callback: CALLBACK
};

export default routes;
