// Global
const HOME = "/";
const LOGIN = "/login";
const LOGOUT = "/logout";
const GOOGLE = "/google";

const routes = {
  home: HOME,
  google: GOOGLE,
  login: LOGIN,
  logout: LOGOUT
};

export default routes;
